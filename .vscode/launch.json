{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run classifier",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/classifier.py",
            "console": "integratedTerminal"
        }
    ]
}